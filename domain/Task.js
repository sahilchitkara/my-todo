/*
 * Define the Schema of the collection (MongooseJS schema definition)
 * */
exports.schema = {
    userId:String,
    title: String,
    description:String,
    timeStampCreated: Number,
    dueDate:String,
    status:String
}

